{
  "name": "gssam-web",
  "version": "0.1.0",
  "description": "Browser tool that converts switching DC-DC converters into a closed-loop GSSAM averaging model (ABCD).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory public",
    "test": "echo 'Wire in vitest / node --test once the math lands' && exit 0",
    "lint": "echo 'Lint not configured yet' && exit 0"
  },
  "keywords": [
    "GSSAM",
    "state-space-averaging",
    "DC-DC",
    "buck",
    "boost",
    "buck-boost",
    "power-electronics"
  ],
  "license": "MIT"
}
