/**
 * Results panel — renders the ABCD matrices, eigenvalues, and (eventually) plots.
 *
 *   renderResults(container, { A, B, C, D, op, parameters });
 */

export function renderResults(container, result) {
  // TODO: render result.A as a table, result.B as a column, op as a list,
  // eigenvalues from src/core/analysis.js when available
  throw new Error('renderResults: not implemented yet');
}
